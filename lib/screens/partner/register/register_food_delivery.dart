import 'package:flutter/material.dart';

class RegisterFoodDelivery extends StatelessWidget {
  static String routeName = "/register_food";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Register as Food Delivery'),
      ),
    );
  }
}
